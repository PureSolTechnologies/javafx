package com.puresoltechnologies.javafx.charts.renderer;

import com.puresoltechnologies.javafx.charts.plots.Axis;

import javafx.scene.canvas.Canvas;

public class NominalAxisRenderer extends AbstractAxisRenderer<Object> {

    public NominalAxisRenderer(Canvas canvas, Axis<Object> axis) {
	super(canvas, axis);
    }

}
